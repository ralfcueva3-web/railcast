Model artifacts are generated by the training scripts.

```bash
python ml/train_xgboost.py
python ml/train_lstm.py
```

XGBoost creates one artifact per route segment. LSTM creates `lstm_model.h5` plus `lstm_scaler.pkl`.

Do not commit production model artifacts directly to source control; publish them to a versioned model/artifact registry.
